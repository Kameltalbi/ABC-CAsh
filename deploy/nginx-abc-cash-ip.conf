# Site Nginx dédié — requêtes Host: 213.130.144.183 → API ABC Cash
# N'impacte pas les autres vhosts (mail, domaines, etc.)
server {
    listen 80;
    listen [::]:80;
    server_name 213.130.144.183;

    include snippets/abc-cash.conf;

    location / {
        return 404;
    }
}
